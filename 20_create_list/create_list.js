let cities = ["Karachi", "Lahore", "Islamabad", "Quetta", "Peshawar"]

for(let list = 1; list<=cities.length; list++)
    {
    for (var i =0; i<list-1; i++ ){
        
    }
    console.log(`${list}\t ${cities[i]} `  )
}

